#include <iostream>
#include <vector>
#include "chapter6.h"
using std::cin; using std::cout; using std::endl;
bool compare(const Matrix& a, const Matrix& b);

vector<int>::itertor it change_val(int, vector<int>);
int main(){

    return 0;
}